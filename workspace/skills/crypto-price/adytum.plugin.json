{
  "id": "crypto-price",
  "name": "Crypto Price",
  "description": "Fetch the current price for a crypto asset via CoinGecko.",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "defaultFiat": { "type": "string", "default": "usd", "description": "Fiat currency to quote in (e.g., usd, eur)" },
      "timeoutMs": { "type": "number", "default": 8000, "description": "HTTP timeout in milliseconds" }
    }
  },
  "metadata": {
    "requires": {
      "env": []
    }
  }
}
